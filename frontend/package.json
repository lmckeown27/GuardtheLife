{
  "name": "guardthelife-frontend",
  "version": "1.0.0",
  "description": "GuardtheLife platform - React Web + Expo Mobile",
  "private": true,
  "workspaces": [
    "web",
    "mobile",
    "shared"
  ],
  "scripts": {
    "web:start": "cd web && npm start",
    "web:build": "cd web && npm run build",
    "mobile:start": "cd mobile && npm start",
    "mobile:android": "cd mobile && npm run android",
    "mobile:ios": "cd mobile && npm run ios",
    "mobile:web": "cd mobile && npm run web",
    "mobile:build:all": "cd mobile && npm run build:all",
    "mobile:build:android": "cd mobile && npm run build:android",
    "mobile:build:ios": "cd mobile && npm run build:ios",
    "mobile:build:web": "cd mobile && npm run build:web",
    "install:all": "npm install && cd web && npm install && cd ../mobile && npm install && cd ../shared && npm install",
    "type-check": "cd web && npm run type-check && cd ../mobile && npm run type-check",
        "lint": "cd web && npm run lint && cd ../mobile && npm run lint"
  }
}
